// Concatenate Names

// Решение:
function solve(first_name, second_name, delimiter){
    console.log(`${first_name}${delimiter}${second_name}`);
}

//Извеждане на резултат:
solve('John', 'Smith', '->');
